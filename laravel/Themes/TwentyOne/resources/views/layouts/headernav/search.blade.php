<div  class="relative">



	<livewire:headernav.search-bar />

	@php
	/*


	<div class="flex items-center justify-between h-10 max-w-lg gap-4 px-4 mx-auto transition-colors bg-gray-100 rounded-lg search-box hover:bg-gray-50 focus:bg-gray-50">
		<input type="text" placeholder="Search here..." x-model.debounce.500ms="search" class="block w-full p-0 bg-transparent border-none outline-none focus:outline-none focus:ring-0" />
		<x-heroicon-o-magnifying-glass class="w-6 h-6" />
	</div>
	<div x-cloak x-show="search.length > 0" class="absolute inset-0 z-50 top-16 h-min max-h-[80vh] max-w-lg p-2 space-y-2 overflow-hidden text-sm border border-white rounded-lg search-results bg-gray-50/85 backdrop-blur overflow-y-auto">
		{{-- Markets --}}
		<div class="space-y-4" x-show="filteredMarkets.length > 0" x-cloak>
			<div class="flex items-center justify-between px-2">
				<span class="text-sm text-gray-400">Markets</span>
				<a href="/search?q=&amp;type=markets" class="text-sm text-blue-500" x-text="'View all markets ('+filteredMarkets.length+')'"></a>
			</div>
			<ul class="space-y-1">
				<template x-for="(item, index) in filteredMarkets" :key="index">
					<li>
						<a class="block p-2 text-base font-semibold transition rounded hover:bg-white hover:no-underline" :href="item.slug" x-text="item.title"></a>
					</li>
				</template>
			</ul>
		</div>
		{{-- Tags --}}
		<div class="space-y-4" x-show="tags.length > 0" x-cloak>
			<div class="flex items-center justify-between px-2">
				<span class="text-sm text-gray-400">Tags</span>
				<a href="/search?q=&amp;type=tags" class="text-sm text-blue-500" x-text="'View all tags ('+tags.length+')'"></a>
			</div>
		</div>
		<ul class="flex flex-wrap gap-1">
			<template x-for="(tag, index) in tags" :key="index">
				<li>
					<a class="inline-block p-2 text-xs uppercase transition-colors rounded hover:text-blue-500 bg-white/90 hover:bg-white" :href="'#'+tag.slug" x-text="'#'+tag.name"></a>
				</li>
			</template>
		</ul>
	</div>


	 */
	 @endphp


</div>
